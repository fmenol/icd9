from setuptools import setup, find_packages

setup(
    name='simple_icd10cm',
    version='0.1.0',
    description='A simple Python package for querying ICD-10-CM codes',
    author='Your Name',
    packages=find_packages(),
    python_requires='>=3.7',
    install_requires=[],
) 